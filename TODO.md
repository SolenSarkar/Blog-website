# Fix Contact Info Visibility

- [x] Identify the cause: `data-reveal` hides `.contact-info` by default, but JS observer misses it.
- [x] Edit `script.js` to observe all existing `[data-reveal]` elements.
- [x] Verify fix by reading `script.js`.

